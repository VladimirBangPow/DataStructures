#ifndef TEST_CSTRINGS_H
#define TEST_CSTRINGS_H

/**
 * A driver function that calls individual test functions
 * to verify the correctness of the string library.
 * This function does NOT define main(). It's intended to be called
 * from some other place that has main().
 */
void testCString(void);

#endif // TEST_CSTRINGS_H
